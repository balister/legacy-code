CXXFLAGS = -Wall -O3 -g

bin_PROGRAMS = decimator
bindir = $(prefix)/dom/bin

Decimator_SOURCES = main.cpp decimator.cpp decimator.h

ossieName = decimator
xmldir = $(prefix)/dom/xml/$(ossieName)
dist_xml_DATA = $(ossieName).scd.xml $(ossieName).spd.xml $(ossieName).prf.xml

